# HTM Processor

Приложение для обработки отчётов в формате .HTM и автоматического заполнения файлов .01.

## Описание

Программа извлекает данные из HTML-файлов (секции "Сравнение"), вычисляет арифметические выражения и записывает результаты в текстовые файлы формата .01.

## Возможности

- 🎯 Автоматическое извлечение данных из HTM файлов
- 🧮 Вычисление арифметических выражений (+, -, *, /)
- 📁 Drag & Drop интерфейс
- 💾 Создание копии результата (не перезаписывает исходный файл)
- 🔍 Подробный лог обработки

## Установка и запуск

### Windows

#### Вариант 1: С установленным Python 3.7+

1. Скачайте и распакуйте архив
2. Двойной клик по `run.bat`
3. Программа автоматически установит зависимости и запустится

#### Вариант 2: Python не установлен

1. Скачайте Python с https://www.python.org/downloads/
2. При установке поставьте галочку **"Add Python to PATH"**
3. Следуйте инструкциям из Варианта 1

### Ручная установка

```bash
pip install tkinterdnd2
python main.py
```

## Использование

1. Запустите программу (`main.py` или `run.bat`)
2. Перетащите файлы .HTM и .01 в окно программы  
   *или используйте кнопки выбора файлов*
3. Нажмите **"Обработать"**
4. Результат сохранится как `{имя}_result.01`

## Пример

**Входные данные:**

- `input.HTM` - отчёт с секциями сравнения
- `412.01` - файл с нулевыми значениями

**Результат:**

- `412_result.01` - файл с заполненными значениями из HTM

## Структура проекта

```
htm_processor/
├── main.py           # GUI с drag & drop
├── parser.py         # Парсинг HTM файлов
├── processor.py      # Обработка файлов .01
├── calculator.py     # Вычисление выражений
├── requirements.txt  # Зависимости
├── run.bat          # Автозапуск для Windows
└── INSTALL.txt      # Инструкция по установке
```

## Сборка .exe для Windows

```bash
pip install pyinstaller
pyinstaller --onefile --windowed --name "HTM_Processor" main.py
```

Готовый файл появится в `dist/HTM_Processor.exe`

**Примечание:** Сборка .exe должна выполняться на Windows-машине.

## Автоматическая сборка через GitHub Actions

При push в ветку `main` автоматически собирается .exe файл для Windows.  
Скачать можно во вкладке **Actions** → выбрать последний запуск → **Artifacts**.

## Технологии

- **Python 3.7+**
- **tkinter** - GUI
- **tkinterdnd2** - Drag & Drop
- **PyInstaller** - сборка .exe

## Лицензия

MIT License

## Поддержка

При возникновении проблем создайте Issue на GitHub.
